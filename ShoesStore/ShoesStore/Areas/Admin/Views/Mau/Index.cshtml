@{
    ViewData["title"] = "Color page";
}
@model List<Mau>
<link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">

<link rel="stylesheet" href="path/to/font-awesome/css/all.min.css" />

<div class="text-center">
    <h2 class="display-4">COLOR LIST</h2>
</div>
@section searchArea{
    <form id="searchForm" asp-controller="Mau" asp-action="Index" method="get" class="d-flex" style="width:468px">
        <div class="input-group">
            <!-- Search bar -->
            <input name="searchString" id="searchString" type="search" placeholder="searching color..." class="form-control rounded" aria-label="Search" style="width:100px">
            <button class="btn btn-info rounded" type="submit" style="width: 40px; height: 40px;">
                <i class="fas fa-search"></i>
            </button>
        </div>
    </form>

}
    <!-- Link for creating a new Loai -->
<div style="display: flex; justify-content: space-between; align-items: center; width:100%">
    <!-- Link for creating a new Loai -->
    <a asp-action="Create" asp-controller="Mau" class="btn btn-primary"><i class="mdi mdi-plus-circle"></i></a>
</div>

<div class="table-responsive">
    <table class="table table-striped table-bordered text-center">
        <thead>
            <tr>
                <th scope="col">Color Id</th>
                <th scope="col">Color Name</th>               
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            <!-- Populate table with Loai data -->
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <div style="width: 30px; height: 30px; background-color: @item.Mamau;margin:auto"></div>
                     </td>
                    <td>@item.Tenmau</td>                   
                    <td>
                        <a asp-action="Edit" asp-controller="Mau" asp-route-id="@item.Mamau" class="btn btn-gradient-primary">
                            <i class="mdi mdi-table-edit"></i>
                        </a>
                        <form asp-controller="Mau" asp-action="Delete" asp-route-id="@item.Mamau" method="post" class="d-inline">
                            <button type="submit" class="btn btn-gradient-danger">
                                <i class="mdi mdi-delete"></i>
                            </button>
                        </form>
                    </td>
                    
                </tr>
            }
        </tbody>
    </table>
</div>
