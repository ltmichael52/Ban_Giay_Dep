@using ShoesStore.Models
@model List<Phieumua>
@{
    ViewData["title"] = "Phieumua";
}

<link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">

<link rel="stylesheet" href="path/to/font-awesome/css/all.min.css" />

<div class="text-center">
    <h2 class="display-4">ORDER LIST</h2>
</div>

@section searchArea{
    <form id="searchForm" asp-controller="PhieuMuaAdmin" asp-action="Index" method="get" class="d-flex" style="width:468px">
        <div class="input-group">
            <!-- Search bar -->
            <input name="searchString" id="searchString" type="search" placeholder="Searching phieu mua..." class="form-control rounded" aria-label="Search" style="width:100px">
            <button class="btn btn-info rounded" type="submit" style="width: 40px; height: 40px;">
                <i class="fas fa-search"></i>
            </button>
        </div>
    </form>

}
<div style="display: flex; justify-content: space-between; align-items: center; width:100%">
    <!-- Link for creating a new Loai -->
</div>
<div class="table-responsive">
    <table class="table table-striped table-bordered text-center">
        <thead>
            <tr>
                <th scope="col">Index</th>
                <th scope="col">Order Date</th>
                <th scope="col">Customer</th>
                <th scope="col">Purchase method</th>
                <th scope="col">Total Bill</th>
                <th scope="col">Note</th>
                <th scope="col">State</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            <!-- Populate table with Loai data -->
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Mapm</td>
                    <td>@item.Ngaydat</td>
                    <td>@item.Makh</td>
                    <td>@item.Mapttt</td>
                    <td>@($"{item.Tongtien:#,##0}₫")</td>
                    <td>@item.Ghichu</td>
                    <td>@item.Tinhtrang</td>

                    <td>
                        @* <a asp-action="Partial_SanPham" asp-controller="PhieuMuaAdmin" asp-route-id="@item.Mapm" class="btn btn-gradient-info">
                            <i class="mdi mdi-information"></i>
                        </a> *@
                        @* <a href="#" data-id="@item.Mapm" class="btn btn-gradient-primary btnCapNhat" onclick="return false;">
                            <i class="mdi mdi-table-edit"></i>
                        </a> *@

                        <a asp-action="Edit" asp-controller="PhieuMuaAdmin" asp-route-id="@item.Mapm" class="btn btn-gradient-primary">
                            <i class="mdi mdi-table-edit"></i>
                        </a> 
    
                    </td>

                </tr>
            }
        </tbody>
    </table>
</div>

@* <div class="modal fade" id="modal-default">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Cập nhật trạng thái</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <input type="hidden" id="txtUrunId" value="8" />
                <div class="form-group">
                    <label>Trạng thái</label>
                    <select class="form-control" id="ddTrangThai">
                        <option value="1">Đã thanh toán</option>
                        <option value="2">Hủy</option>
                    </select>
                </div>
            </div>
            <div class="modal-footer justify-content-between">
                <button type="button" class="btn btn-default" data-dismiss="modal">Đóng</button>
                <button type="button" class="btn btn-primary btnLuu">Lưu</button>
            </div>
        </div>
    </div>
</div>
 *@
 @* @section scripts{
    <script>
        $(document).ready(function () {
            $('body').on('click', '.btnCapNhat', function () {
                var id = $(this).data("id");
                $('#txtUrunId').val(id); // Thay đổi từ txtOrderId thành txtUrunId
                $('#modal-default').modal('show');
            });

            $('body').on('click', '.btnLuu', function () {
                var id = $('#txtUrunId').val(); // Thay đổi từ txtOrderId thành txtUrunId
                var tt = $('#ddTrangThai').val(); // Sử dụng ID của select box
                $.ajax({
                    url: '/admin/order/UpdateTT',
                    type: 'POST',
                    data: { id: id, trangthai: tt },
                    success: function (res) {
                        if (res.Success) {
                            location.reload();
                        }
                    }
                });
            });
        });

    </script>
} 

@section scripts{
    <script>
        $(document).ready(function () {
            // Xử lý sự kiện khi nhấn nút cập nhật
            $('body').on('click', '.btnCapNhat', function () {
                var id = $(this).data("id");
                $('#txtUrunId').val(id);
                $('#modal-default').modal('show');
            });

            // Xử lý sự kiện khi nhấn nút lưu trong modal
            $('body').on('click', '.btnLuu', function () {
                var id = $('#txtUrunId').val();
                var tt = $('#ddTrangThai').val();
                $.ajax({
                    url: '/admin/order/UpdateTT',
                    type: 'POST',
                    data: { id: id, trangthai: tt },
                    success: function (res) {
                        if (res.Success) {
                            location.reload();
                        }
                    }
                });
            });
        });
    </script>
} *@

